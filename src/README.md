# src/

Source files that get compiled into public assets.

## Files

- `input.css` — Tailwind CSS entry point (`@import "tailwindcss"`)

## Build Commands

```bash
npm run dev     # Watch mode — recompiles on save
npm run build   # Production — minified output to public/css/app.css
```
